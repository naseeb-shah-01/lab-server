import { Router } from 'express';
// import { addUser, getUsers, getUserById, deleteUser, updateUser } from '../../../controllers/users/users';
 const router = Router();

// router.get('/', (req, res) => {
//     res.handle(getUsers, req.query, 'list');
// });

// router.get('/:id', (req, res) => {
//     res.handle(getUserById, req.params.id);
// });

// router.post('/', (req, res) => {
//     res.handle(addUser, [req.body, req.getUser()]);
// });

// router.put('/:id', (req, res) => {
//     res.handle(updateUser, [req.params.id, req.body, req.getUser()]);
// });

// router.delete('/:id', (req, res) => {
//     res.handle(deleteUser, [req.params.id, req.getUser()]);
// });

export default router;
